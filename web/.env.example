# Waitlist delivery (optional).
# Without these the API validates the signup and logs it server-side, but does
# not email anyone. Set all three to deliver signups by email via Resend.
RESEND_API_KEY=
WAITLIST_FROM_EMAIL="WeNaija <waitlist@wenaija.ng>"
WAITLIST_TO_EMAIL=hello@wenaija.ng
